{
  "buildCommand": "pnpm --filter @workspace/web-app run build",
  "outputDirectory": "artifacts/web-app/dist/public",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "env": {
    "PORT": "3000",
    "BASE_PATH": "/"
  }
}
