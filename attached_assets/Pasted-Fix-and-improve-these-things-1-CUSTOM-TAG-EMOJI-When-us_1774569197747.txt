Fix and improve these things:
	1.	CUSTOM TAG EMOJI: When user types a custom tag word in Settings, automatically suggest a fitting emoji using keyword-to-emoji mapping. The final saved tag must always be a combination of emoji + word (e.g. “🏖️ Beach”, “💪 Gym”, “🎉 Party”). Show the suggested emoji next to the typed word before saving. User can change the emoji before confirming. All existing tags must also follow this emoji + word format.
	2.	SINGLE POST UI: The Single Post window (both AI and manual) must look and function EXACTLY like the Carousel window in terms of design, layout, and style. Same dark card design, same caption section with options, same user caption ideas input, same Save as Draft button, same 4:5 image ratio. Only difference: one image slot, with “Choose Image Myself” and “Let AI Choose” buttons instead of the filmstrip.
	3.	CAROUSEL PREFERENCES IN SETTINGS: Consolidate all carousel AI rules into one clear “Carousel Preferences” section in Settings. Include: Carousel Size (number 2-20 or Random). Slide order rules as toggleable options: “Always put a Me image first” (default ON), “Follow tag sequence” (user defines sequence like Me → Vibe → Food → Location → Outfit), “AI chooses freely” (overrides all sequence rules). If no rules are set, AI picks images that fit together by mood and tags. Merge with existing AI preferences to avoid duplication.
	4.	CAPTION GENERATION - OVERALL VIBE: Completely rewrite the caption generation prompt. The AI must NOT describe each image individually. The prompt must say: “You are writing ONE Instagram caption for a carousel post. Do not describe individual images. Instead capture the overall mood, feeling, and vibe of the post as a whole. Write as if you are the person in the photos expressing how the moment felt. Keep it short, cool, lowercase, maximum 1-2 sentences plus maximum 2 hashtags.” Pass the list of image tags as context so AI understands the mix.
	5.	CAPTION PROMPT IN SETTINGS: In Settings under Caption Style, add a visible editable text field labeled “Caption Prompt” that contains the full prompt text. Pre-fill it with exactly this text: “You are writing ONE Instagram caption for a carousel post. Do not describe individual images. Instead capture the overall mood, feeling, and vibe of the post as a whole. Write as if you are the person in the photos expressing how the moment felt. Keep it short, cool, lowercase, maximum 1-2 sentences plus maximum 2 hashtags.” Every user can edit this prompt individually to customize how their captions are generated. This prompt is injected directly into every caption generation API call.