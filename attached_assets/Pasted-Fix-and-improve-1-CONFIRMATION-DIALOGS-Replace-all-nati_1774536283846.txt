Fix and improve:
	1.	CONFIRMATION DIALOGS: Replace all native browser alert/confirm dialogs (like “Delete this folder?” and “Remove from folder?”) with custom styled modals that match the app’s dark design. Use the same dark background, rounded corners, and button style as the rest of the app. Never use native browser dialogs.
	2.	FOLDER VIEW - ADD MEDIA BUTTON: Inside an open folder, add an empty dashed-border square with a “+” and “Add Media” label as the last item in the grid, same style as the carousel filmstrip add button.
	3.	MEDIA ITEM TAP BEHAVIOR: When tapping a single media item in the pool (with nothing else selected), open a full-screen image viewer showing the image at full size. Add a small action bar at the bottom of the full-screen view with 3 buttons: “Add to Carousel”, “Single Post”, “Delete from Pool”. Long-press still enters bulk selection mode.
	4.	TODAY’S SECTION - SCHEDULED POSTS: If a post is scheduled for today, show it in the Today section as an Instagram-style preview card that can also be edited. Same presentation as in the calendar.
	5.	TODAY’S SECTION - DEFAULT TIME: When adding a slide in Today’s section and scheduling, set the default time and date to the current time and date, not 12:00.
	6.	AI CAROUSEL - RULE BASED TOGGLE: Add a toggle (on/off) for “Rule-based” when creating an AI carousel. When ON, the AI follows the rules (first slide = “me” tag, varied mix). When OFF, AI picks freely from selected source. The toggle should work alongside tag selection and folder selection.
	7.	DESCRIBE A MOMENT - REMOVE: Remove the “Describe a moment” option from AI carousel creation entirely. Replace it with “From Folder” as the third source option (alongside “All Media” and “By Tag”). This is more accurate since the user organizes moments into folders.
	8.	USED SECTION - NAVIGATION FIX: When user clicks “Edit” on a post in Used, then clicks “Cancel” inside the editor, redirect back to Media Pool → Used tab, not to the Calendar.
	9.	USED SECTION - DELETE: Add a Delete button to each post listed in the Used section. Also add a Delete button inside the post editing screen. Deleting a post from Used moves its media files back to “All”.