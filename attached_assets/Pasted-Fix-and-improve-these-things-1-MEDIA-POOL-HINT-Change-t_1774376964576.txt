Fix and improve these things:
	1.	MEDIA POOL HINT: Change the hint text to “Select 2–20 items” (minimum 2, maximum 20).
	2.	POST PREVIEW: Change aspect ratio from 1:1 to 4:5. Fix the action icons row to use proper SVG icons (not emojis): heart icon, speech bubble icon (comment), paper plane icon (send/share), bookmark/tag icon on the right - all as clean line icons, same style as real Instagram.
	3.	DRAG AND DROP: When user long-presses a carousel thumbnail, scale it DOWN slightly (e.g. 0.9x) instead of up - then it floats and drags like iOS home screen. Keep the rest of the behavior the same.
	4.	CAROUSEL THUMBNAIL STRIP: The horizontal scrollable thumbnail strip with the dashed “+” Add Media box at the end must appear on ALL carousel views - Today’s carousel, AI-generated carousels, manually built carousels, and when editing any existing carousel. It must NOT appear on the Today empty state. The strip must be horizontally swipeable/scrollable. The “+” dashed box must always be the last item. Move it out of the Today empty state and into the actual carousel slide strip only.
	5.	USED MEDIA BUG: Fix the bug where used images still show in “All” after reload, and where one image can be used in multiple posts. Each media item must only belong to one post at a time. When app loads, re-validate: any media marked “used” that has no corresponding active scheduled post must be moved back to “All”. Prevent the same media item from being added to a second post if it is already used.
	6.	USED FILTER - INSTAGRAM PREVIEW: Add Instagram-style preview option inside the Used filter, same as in the Calendar view.
	7.	UPDATE POST BUG: When user edits an existing scheduled post and clicks “Update Post”, it must overwrite the existing post - not create a new one. Fix this so the original post is updated in place with the new changes.
	8.	CANCEL BUTTON: While any AI generation is in progress (caption, carousel suggestion, AI post), always show a visible “Cancel” button that stops the generation and returns the user to the previous state.