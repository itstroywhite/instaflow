Fix and improve these things:
	1.	VIDEO TAGGING ON UPLOAD: When multiple videos are uploaded at once, show a sequential tagging flow - display each video one at a time with a small video player so the user can see what it is, then select the tag below it, then move to the next video. Never show all videos at once for tagging.
	2.	FOLDER - ADD FROM CAMERA ROLL: When adding media to a folder (including empty folders), offer two options: “From Pool” (already uploaded media) and “From Camera Roll” (device photos/videos). Camera roll uploads go through the normal processing and tagging flow.
	3.	MEDIA POOL - iOS GALLERY STYLE SELECTION: Add a “Select” button to the media pool header. When tapped, enters selection mode where tapping any media item selects it (shows checkmark). When one or more items are selected, show a bottom action bar with: “Add to Folder”, “Delete”, “Create Post”. Tapping the same item again deselects it. A “Cancel” button exits selection mode. Single tap on media (outside selection mode) still opens the fullscreen iOS-style viewer. Keep all existing filters, sort, new folder button, and All/Used tabs exactly as they are.
	4.	EMPTY FOLDER ITEM COUNT BUG: Folders with no media should show “0 items” or nothing, not a wrong number. Fix the item count calculation for folders.
	5.	VIDEO UPLOAD PERFORMANCE: Multiple video uploads crash or slow down the app. Process videos one at a time sequentially instead of in parallel. Show a progress indicator per video. Do not block the UI during upload.
	6.	FOLDER - REMOVE FROM FOLDER: Inside a folder, when long-pressing a media item, add “Remove from Folder” as an option. This moves the item back to “All” in the pool without deleting it. Keep “Delete from Pool” as a separate destructive option.
	7.	CAPTION CREATION - USER IDEAS INPUT: When generating captions, add a text input field where user can type their own ideas or keywords (e.g. “mention the rooftop”, “reference the music”, “add something about the night”). This input is injected into the AI caption prompt so the generated captions incorporate the user’s ideas.