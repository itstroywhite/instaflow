Fix and improve these things:
	1.	CAMERA ROLL + CAMERA OPTIONS: When adding media to a carousel, show two separate options: “Camera Roll” (opens photo library) and “Take Photo/Video” (opens camera). Both options must appear wherever media can be added to a carousel.
	2.	DUPLICATE MEDIA: Delete all duplicate media files from the database now (same filename or same file size = duplicate). Add a check on upload: if the same file already exists in the pool, skip it and show a message “Already in your pool”.
	3.	FULLSCREEN IMAGE - NAVIGATION FIX: When viewing a full image and opening the tag menu, after closing the tag menu return to the fullscreen image view, not the pool grid. Same for other options that open sub-menus from the fullscreen view - always return to fullscreen after closing sub-menus.
	4.	SINGLE POST - SAME BOARD AS CAROUSEL: When creating a Single Post (manually or with AI), use the exact same UI board as carousel creation: 4:5 image ratio, caption suggestions, user caption ideas input, save as draft option. Only difference: one image at a time. Add two buttons: “Choose Next Image Myself” (opens pool picker) and “Let AI Choose” (AI picks the best matching image from pool based on AI preferences). User can switch between manual and AI image selection at any point.
	5.	FOLDER MEDIA SELECTION: When adding media to a folder (including empty folders), do not auto-add on tap. Instead show a selection picker where user ticks multiple files, then confirms with an “Add to Folder” button.
	6.	AI CAROUSEL SLIDE COUNT: Add a setting in Settings called “Carousel Size” with options: specific number (2-20 slider or input) and “Random” (AI picks between 2-20 based on what fits). When Random is selected, AI analyzes which media files fit together by tag and AI preferences to decide the count. Currently AI always picks 6 - fix this to respect the setting.
	7.	CUSTOM TAGS - EMOJI SUPPORT: In Settings where user can add custom tags, allow emojis in the tag name input field.
	8.	TIMEZONE FIX: Use the device’s local timezone automatically for all date and time displays. Do not hardcode any timezone. Use JavaScript’s Intl.DateTimeFormat with the user’s local timezone from the browser. Fix the current date display - it must show today’s correct date based on device timezone.
	9.	MAIN MENU RENAME: In the “+” main menu, rename “Build Carousel Myself” to “Build Carousel”.